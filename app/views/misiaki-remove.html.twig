{% extends 'base.html.twig' %}
{% set page_title = 'Usuń log members' %}
{% block content_class_name %}members{% endblock %}

{% block content %}
<div id="members-log-add-section">

    Tak naprawdę to logi nie są usuwane. Zamiast tego dostają flagę "deleted" i nie są pokazywane w listingach.
    Tak więc bez strachu można usuwać - w razie pomyłki można bez problemu przywrócić.
    <br /><br />
    {% for log in logs %}
        {% if log.archived %}
            <div>{{ log.filename }} (<a href="{{ path('misiaki-restore', {'log': log.filename|trim('.txt.deleted')|trim('mem-')}) }}">przywróć</a>)</div>
        {% else %}
            <div>{{ log.filename }} (<a href="{{ path('misiaki-remove', {'log': log.filename|trim('.txt')|trim('mem-')}) }}">usuń</a>)</div>
        {% endif %}
    {% endfor %}
</div>
{% endblock %}
